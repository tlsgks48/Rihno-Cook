<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowFullscreen">true</item>

        <item name="android:itemBackground">@android:color/white</item>
        <item name="android:itemTextAppearance">@style/MenuText</item>
    </style>

    <style name="MenuText">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="TabTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--배경-->
        <item name="colorPrimary">#2ca388</item>
        <!--탭마 선택된 텍스트-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--탭바 텍스트-->
        <item name="android:textColorSecondary">@android:color/black</item>
        <!--하단 움직이는 바 색-->
        <item name="colorAccent">@android:color/white</item>

    </style>

    <style name="SplashScreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/rihno2</item>
    </style>


</resources>
